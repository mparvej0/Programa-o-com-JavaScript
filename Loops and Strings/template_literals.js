let specialString = `This is a template literal`;
console.log(specialString, typeof specialString);

// Stargint Interpolation
let obj = {
    item: "pen",
    price: 10
};
let Output = `The cost of ${obj.item} is ${obj.price} rupees`;
console.log(Output);